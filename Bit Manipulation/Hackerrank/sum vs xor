long long  sumXor(long long n) {
   long long count = 0;  
   while(n>0)
   {
if(!(n&1))count++;
n=n>>1;

   }
   return count;
}

int main()
{
    long long  n;
    cin>>n;
    cout<<fixed;
   printf("%0.f",pow(2,sumXor(n)));

    return 0;
}
